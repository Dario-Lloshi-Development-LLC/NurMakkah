# Android Package / applicationId Audit

Summary of occurrences related to Android package, namespace, and applicationId found in the repository (no changes made):

- `android/app/build.gradle`
  - `namespace "com.nurmakkah"`
  - `applicationId "com.nurmakkah"`
- `android/app/src/main/AndroidManifest.xml`
  - `<data android:scheme="nurmakkah" />`
- `android/app/src/main/res/values/strings.xml`
  - `<string name="app_name">Nur Makkah</string>`
- `android/app/src/main/java/com/nurmakkah/MainActivity.java`
  - `package com.nurmakkah;`
- `android/app/src/main/java/com/nurmakkah/MainApplication.java`
  - `package com.nurmakkah;`
- `android/settings.gradle` and `settings.gradle`
  - `rootProject.name = 'NurMakkah'` / `"NurMakkah"`
- `src/index.tsx`
  - `AppRegistry.registerComponent('NurMakkah', () => App);`
- `.github/workflows/ci-cd-react-native.yml`
  - CI references `NurMakkah` in Xcode scheme and related steps

Notes & recommendations:
- No automated renames were performed on native package statements in this audit (intentional: high risk).
- If you plan to change Play Store package (`applicationId`) or Java/Kotlin package names, do this as a separate atomic change with tests and Play Store migration plan.
- Double-check any CI/CD secrets, manifest placeholders, and Play Store entries before modifying `applicationId`.
- If you just need branding updates (app_name, UI strings), prefer changing resources and JS/TS constants rather than native packages.

If you want, I can:
- Prepare an atomic PR that renames Java/Kotlin packages and updates Gradle `applicationId` (I will not perform this without explicit approval), or
- Update UI-level branding strings only (safe, low-risk), or
- Produce a checklist for Play Store migration when changing `applicationId`.

Generated by automation during `feature/modernization` audit.
